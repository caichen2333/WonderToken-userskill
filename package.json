{
  "name": "wondertoken-skill-repository",
  "version": "0.8.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.14.0 <25"
  },
  "scripts": {
    "validate": "node scripts/validate-skill.mjs",
    "test": "node --import tsx --test --test-concurrency=4 test/*.test.ts",
    "install:dependencies": "npm --prefix skills/wondertoken ci --omit=dev --ignore-scripts --no-audit --no-fund"
  },
  "devDependencies": {
    "tsx": "4.20.5"
  }
}

